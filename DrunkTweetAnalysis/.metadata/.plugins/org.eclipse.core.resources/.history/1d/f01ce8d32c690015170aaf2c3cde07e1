/*
@author ranjanmanish
this simple java code takes the two text files generated by python code to
create a unique user list.
PS: did not do in python as it was easy to code in java for me
 */
import java.io.*;
import java.util.*;
/**
 * thisclass takes a {user:#:text} document and gets just the text after removing urls
 * @author manish
 *
 */
class WordFreq{
	public static void main(String args[]){
		String fileName = "/home/manish/fall2015/RA/DrunkTweetAnalysis/spark/test.txt";
		Map<Integer, String> map = new TreeMap<Integer, String>();
		try{

			//Create object of FileReader
			FileReader inputFile = new FileReader(fileName);

			//Instantiate the BufferedReader Class
			BufferedReader bufferReader = new BufferedReader(inputFile);

			//Variable to hold the one line data
			String line;
			String[] arr;
			// Read file line by line and print on the console
			while ((line = bufferReader.readLine()) != null)   {
				System.out.println(line);
				
				#map.put(Integer.parseInt(arr[1]), arr[0]);
			}
			bufferReader.close();
		}
		catch(Exception e){
			System.out.println("Error while reading file line by line:" + e.getMessage());
		}
		for (Map.Entry<Integer, String> entry : map.entrySet()) {
		    System.out.println(entry.getKey()+" : "+entry.getValue());
		}
	}

}
